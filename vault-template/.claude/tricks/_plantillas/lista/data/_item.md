---
# Copy this file to add an item by hand. Change `tipo` to `tarea` — the
# manifest's vault.query filter only lists notes with that tipo, which is
# exactly what keeps this template itself out of the list.
titulo: One item, described the way you would say it out loud
tipo: plantilla
estado: activo
actualizado: 2026-08-15
---

Anything below the frontmatter is the item's own note: context, links,
whatever. The checklist app never reads or writes it — `cuerpo: false` in
the manifest means the app cannot touch the body even by accident.
