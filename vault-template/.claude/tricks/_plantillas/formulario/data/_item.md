---
# What the app writes, so you can see the shape without running it. The
# frontmatter keys here are exactly the ones listed in `campos:` in
# trick.yaml — a key that is not listed gets the whole write refused,
# not silently dropped.
titulo: Whatever was typed in the box
tipo: captura
estado: activo
actualizado: 2026-08-15
---

The text from the second field lands here, as the note body. That is
allowed only because `cuerpo: true` is in the manifest.
